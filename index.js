// write your code below!
var name = "Susan"
var height = 39
var message = ` is ${height} inches tall`

module.exports = {name, height, message}

/*
describe('what-is-a-test',() => {
  describe('Name',() => {
  it('returns "Susan"', () => {
    expect(index.name).toEqual('Susan')
  })
})

describe('Hieght', () => {
  it('less than 40', () => {
    expect(index.hieght).toBeLessThan(40)
  })
})

describe('Message', () => {
  it('gives the name and hieght', () => {
    expect(index.name).toInclude(index.name)
    expect(index.hieght).toInclude(index.hieght)
  })
})
})
*/